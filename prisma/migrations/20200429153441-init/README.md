# Migration `20200429153441-init`

This migration has been generated by Miki at 4/29/2020, 3:34:41 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Expense" (
    "amount" integer  NOT NULL ,
    "categoryId" integer  NOT NULL ,
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "createdById" integer  NOT NULL ,
    "id" SERIAL,
    "memo" text   ,
    "paidById" integer  NOT NULL ,
    "paymentOptionId" integer  NOT NULL ,
    "title" text  NOT NULL ,
    "weightedAmount" integer  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."User" (
    "id" SERIAL,
    "name" text   ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Category" (
    "id" SERIAL,
    "name" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."PaymentOption" (
    "id" SERIAL,
    "name" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "Category.name" ON "public"."Category"("name")

CREATE UNIQUE INDEX "PaymentOption.name" ON "public"."PaymentOption"("name")

ALTER TABLE "public"."Expense" ADD FOREIGN KEY ("paidById")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Expense" ADD FOREIGN KEY ("categoryId")REFERENCES "public"."Category"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Expense" ADD FOREIGN KEY ("paymentOptionId")REFERENCES "public"."PaymentOption"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Expense" ADD FOREIGN KEY ("createdById")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200429153441-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,45 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Expense {
+  id        Int      @default(autoincrement()) @id
+  title     String
+  memo   String?
+  amount  Int
+  weightedAmount  Int
+  paidById Int 
+  paidBy   User    @relation("Expense_paidBy", fields: [paidById], references: [id])
+  categoryId Int 
+  category   Category    @relation(fields: [categoryId], references: [id])
+  paymentOptionId Int
+  paymentOption   PaymentOption    @relation(fields: [paymentOptionId], references: [id])
+  createdAt DateTime @default(now())
+  createdById Int 
+  createdBy   User    @relation("Expense_createdBy", fields: [createdById], references: [id])
+}
+model User {
+  id     Int     @default(autoincrement()) @id
+  name    String?
+  payExpenses     Expense[] @relation("Expense_paidBy")
+  createExpenses  Expense[] @relation("Expense_createdBy")
+}
+model Category {
+  id      Int      @default(autoincrement()) @id
+  name   String   @unique
+  expenses     Expense[]
+}
+
+model PaymentOption {
+  id      Int      @default(autoincrement()) @id
+  name   String   @unique
+  expenses     Expense[]
+}
```


